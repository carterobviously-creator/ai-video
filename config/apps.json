{
  "apps": [
    {
      "id": "ai-video-studio",
      "name": "AI Video Studio",
      "description": "Interactive image generation using Stable Diffusion 1.5 ONNX on CPU.",
      "executable": "python",
      "args": "generate.py --interactive",
      "workingDir": "{{scriptRoot}}",
      "notes": "Launches the interactive SD ONNX image generator."
    },
    {
      "id": "local-agent",
      "name": "Local Agent",
      "description": "Headless image generation agent - pass prompts via command line.",
      "executable": "python",
      "args": "generate.py",
      "workingDir": "{{scriptRoot}}",
      "notes": "Run with --prompt 'your prompt' for single-shot generation."
    }
  ]
}
